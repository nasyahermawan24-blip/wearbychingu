export interface RevenueChart {
  month: string;
  revenue: number;
}

export interface StatusChart {
  name: string;
  value: number;
}

export interface ProductChart {
  name: string;
  total: number;
}